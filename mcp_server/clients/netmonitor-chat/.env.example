# NetMonitor Chat - Environment Configuration
# Copy this file to .env and fill in your values
# Do NOT commit .env to git!
#
# OPMERKING: Alle onderstaande instellingen kunnen ook via de web UI geconfigureerd worden!
# De UI configuratie wordt opgeslagen in de browser localStorage.
# Deze environment variables dienen alleen als fallback/defaults.

# Ollama API URL (default voor LLM provider)
OLLAMA_BASE_URL=http://localhost:11434

# MCP Server URL (default)
MCP_SERVER_URL=https://soc.poort.net/mcp

# MCP Authentication Token (default)
# Get your token with:
#   cd /home/user/netmonitor
#   python3 mcp_server/manage_tokens.py create --name "NetMonitor Chat" --scope read_only --rate-minute 120
MCP_AUTH_TOKEN=your_token_here

# MCP Bridge Path (optional - only needed if not using standard layout)
# Mac users: set this to the full path of your mcp_bridge.py file
# Example: MCP_BRIDGE_PATH=/Users/username/path/to/mcp_bridge.py
# MCP_BRIDGE_PATH=

# LM Studio configuratie (optioneel - configureer via web UI)
# LM Studio draait standaard op http://localhost:1234
# Selecteer "LM Studio" als provider in de web UI en gebruik deze URL
